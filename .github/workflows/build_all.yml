name: Generate Android and iOS
on: push
jobs:
  create_runners:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v2
      - run: |
          rm -rf android ios
          flutter create . --project-name=nexus_ui --platforms=android --force
